import 'package:flutter/material.dart';

class MyCities extends StatelessWidget {
  const MyCities({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("hello"));
  }
}
